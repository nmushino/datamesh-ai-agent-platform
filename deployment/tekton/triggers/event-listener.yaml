---
# GitHub Webhook を受信して Tekton パイプラインを起動する
apiVersion: triggers.tekton.dev/v1beta1
kind: EventListener
metadata:
  name: github-event-listener
  namespace: ai-agent-cicd
spec:
  serviceAccountName: tekton-triggers-sa
  triggers:
    # agent/ 配下の変更 → AI Agent パイプライン
    - name: ai-agent-trigger
      interceptors:
        - ref:
            name: github
          params:
            - name: secretRef
              value:
                secretName: github-webhook-secret
                secretKey: secret
            - name: eventTypes
              value:
                - push
        - ref:
            name: cel
          params:
            - name: filter
              value: >
                body.ref.startsWith('refs/heads/') &&
                body.commits.exists(c, c.modified.exists(f, f.startsWith('agent/')) ||
                                       c.added.exists(f, f.startsWith('agent/')) ||
                                       c.modified.exists(f, f.startsWith('tools/')))
      bindings:
        - ref: github-push-binding
      template:
        ref: ai-agent-pipeline-run-template

    # backend/ 配下の変更 → Business API パイプライン
    - name: business-api-trigger
      interceptors:
        - ref:
            name: github
          params:
            - name: secretRef
              value:
                secretName: github-webhook-secret
                secretKey: secret
            - name: eventTypes
              value:
                - push
        - ref:
            name: cel
          params:
            - name: filter
              value: >
                body.ref.startsWith('refs/heads/') &&
                body.commits.exists(c, c.modified.exists(f, f.startsWith('backend/')) ||
                                       c.added.exists(f, f.startsWith('backend/')))
      bindings:
        - ref: github-push-binding
      template:
        ref: business-api-pipeline-run-template

    # frontend/chat-ui/ 配下の変更 → Chat UI パイプライン
    - name: chat-ui-trigger
      interceptors:
        - ref:
            name: github
          params:
            - name: secretRef
              value:
                secretName: github-webhook-secret
                secretKey: secret
            - name: eventTypes
              value:
                - push
        - ref:
            name: cel
          params:
            - name: filter
              value: >
                body.ref.startsWith('refs/heads/') &&
                body.commits.exists(c, c.modified.exists(f, f.startsWith('frontend/chat-ui/')) ||
                                       c.added.exists(f, f.startsWith('frontend/chat-ui/')))
      bindings:
        - ref: github-push-binding
      template:
        ref: chat-ui-pipeline-run-template
---
# GitHub Webhook 受信用の Route
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: github-event-listener
  namespace: ai-agent-cicd
spec:
  to:
    kind: Service
    name: el-github-event-listener
  port:
    targetPort: http-listener
  tls:
    termination: edge
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: tekton-triggers-sa
  namespace: ai-agent-cicd
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: tekton-triggers-rb
  namespace: ai-agent-cicd
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: tekton-triggers-eventlistener-roles
subjects:
  - kind: ServiceAccount
    name: tekton-triggers-sa
    namespace: ai-agent-cicd
---
# EventListener は ClusterInterceptor / ClusterTriggerBinding など
# クラスタスコープのリソースも list/watch する必要があるが、上の RoleBinding
# (namespace スコープ) だけでは付与されず、
# "clusterinterceptors.triggers.tekton.dev is forbidden ... at the cluster scope"
# で informer の初期化に失敗し el-github-event-listener が CrashLoopBackOff に
# なっていた (2026-07-22, sandbox39 で発生・原因調査済み)。
# ClusterRoleBinding で明示的に付与する。
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: tekton-triggers-sa-ai-agent-cicd-clusterroles
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: tekton-triggers-eventlistener-clusterroles
subjects:
  - kind: ServiceAccount
    name: tekton-triggers-sa
    namespace: ai-agent-cicd
