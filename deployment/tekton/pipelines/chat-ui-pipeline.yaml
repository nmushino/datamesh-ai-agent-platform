apiVersion: tekton.dev/v1
kind: Pipeline
metadata:
  name: chat-ui-build-pipeline
  namespace: ai-agent-cicd
  annotations:
    tekton.dev/cicd: "true"
    tekton.dev/displayName: "Chat UI (React/Vite) ビルド・デプロイ パイプライン"
spec:
  params:
    - name: git-url
      type: string
      description: Git リポジトリ URL
    - name: git-revision
      type: string
      default: main
      description: Git ブランチ / タグ / コミットハッシュ
    - name: image-name
      type: string
      # 内部レジストリ (overlays/dev の image remap と一致させる)
      default: image-registry.openshift-image-registry.svc:5000/ai-agent-platform/chat-ui
      description: ビルドするイメージ名
    - name: deploy-overlay
      type: string
      default: deployment/kustomize/overlays/dev
      description: デプロイ先 overlay パス
    - name: enable-gitops-commit
      type: string
      default: "false"
      description: "true の場合のみ GitOps リポジトリへコミット・プッシュする (git-credentials Secret が必要)"
  workspaces:
    - name: shared-workspace
      description: パイプライン全体で共有するワークスペース
    - name: docker-credentials
      description: コンテナレジストリ認証情報
  results:
    - name: image-digest
      value: $(tasks.build-image.results.IMAGE_DIGEST)
  tasks:
    # ① ソースコードを取得
    - name: git-clone
      taskRef:
        resolver: cluster
        params:
          - name: kind
            value: task
          - name: name
            value: git-clone
          - name: namespace
            value: openshift-pipelines
      # NOTE: このクラスターの openshift-pipelines/git-clone Task は
      # tekton catalog 版と異なり大文字のパラメータ名を使う。
      params:
        - name: URL
          value: $(params.git-url)
        - name: REVISION
          value: $(params.git-revision)
        - name: DELETE_EXISTING
          value: "true"
      workspaces:
        - name: output
          workspace: shared-workspace

    # ② コンテナイメージをビルド & プッシュ (Dockerfile 内の npm run build がテスト代わりに型チェックまで行う)
    - name: build-image
      runAfter:
        - git-clone
      taskRef:
        resolver: cluster
        params:
          - name: kind
            value: task
          - name: name
            value: buildah
          - name: namespace
            value: openshift-pipelines
      params:
        - name: IMAGE
          value: $(params.image-name):$(tasks.git-clone.results.COMMIT)
        - name: DOCKERFILE
          value: frontend/chat-ui/Dockerfile
        - name: CONTEXT
          value: frontend/chat-ui
        - name: FORMAT
          value: oci
      workspaces:
        - name: source
          workspace: shared-workspace
        - name: dockerconfig
          workspace: docker-credentials

    # ③ イメージスキャン (Trivy)
    - name: image-scan
      runAfter:
        - build-image
      taskRef:
        name: trivy-scanner
        kind: Task
      params:
        - name: IMAGE
          value: $(params.image-name):$(tasks.git-clone.results.COMMIT)
        - name: SEVERITY
          value: "CRITICAL,HIGH"
      workspaces:
        - name: source
          workspace: shared-workspace

    # ④ Kustomize の image タグを更新して GitOps でデプロイ (任意)
    - name: update-gitops
      runAfter:
        - image-scan
      when:
        - input: $(params.enable-gitops-commit)
          operator: in
          values: ["true"]
      taskRef:
        name: update-kustomize-image
        kind: Task
      params:
        - name: image-name
          value: $(params.image-name)
        - name: new-tag
          value: $(tasks.git-clone.results.COMMIT)
        - name: overlay-path
          value: $(params.deploy-overlay)
      workspaces:
        - name: source
          workspace: shared-workspace

  # パイプライン失敗時の通知 (チャット通知ではなく共有Kafkaのpipeline-notificationsトピックに保持する)
  finally:
    - name: notify-on-failure
      when:
        - input: $(tasks.build-image.status)
          operator: in
          values: ["Failed"]
      taskRef:
        name: send-kafka-notification
        kind: Task
      params:
        - name: message
          value: "Chat UI パイプラインが失敗しました: $(params.git-url) @ $(params.git-revision)"
        - name: pipeline-name
          value: "chat-ui-build-pipeline"
        - name: status
          value: "Failed"
