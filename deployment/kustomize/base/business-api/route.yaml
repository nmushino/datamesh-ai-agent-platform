apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: business-api
  namespace: ai-agent-platform
spec:
  to:
    kind: Service
    name: business-api
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
