apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: openmetadata
  namespace: ai-agent-platform
spec:
  to:
    kind: Service
    name: openmetadata
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
