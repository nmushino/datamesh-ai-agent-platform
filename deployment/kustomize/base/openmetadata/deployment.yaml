apiVersion: apps/v1
kind: Deployment
metadata:
  name: openmetadata
  namespace: ai-agent-platform
spec:
  replicas: 1
  selector:
    matchLabels:
      app: openmetadata
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: openmetadata
    spec:
      # elasticsearch/postgresql 等 Service 名と同名の自動注入環境変数(ELASTICSEARCH_PORT等)が
      # 明示的な env 設定と衝突するため無効化する
      enableServiceLinks: false
      initContainers:
        - name: wait-for-db
          image: busybox:1.36
          command: ['sh', '-c', 'until nc -z postgresql 5432; do sleep 2; done; until nc -z elasticsearch 9200; do sleep 2; done']
        # OpenMetadata サーバーは起動時に自動でスキーマ移行しないため事前に実行する
        - name: db-migrate
          image: docker.getcollate.io/openmetadata/server:1.3.0
          command: ['/opt/openmetadata/bootstrap/bootstrap_storage.sh', 'migrate-all']
          env:
            - name: DB_DRIVER_CLASS
              value: "org.postgresql.Driver"
            - name: DB_SCHEME
              value: "postgresql"
            - name: DB_HOST
              value: "postgresql"
            - name: DB_PORT
              value: "5432"
            - name: OM_DATABASE
              value: "openmetadata_db"
            - name: DB_USER
              value: "postgres"
            - name: DB_USER_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgresql-secret
                  key: password
      containers:
        - name: openmetadata
          image: docker.getcollate.io/openmetadata/server:1.3.0
          ports:
            - containerPort: 8585
          env:
            - name: OPENMETADATA_CLUSTER_NAME
              value: "enterprise-ai-agent"
            - name: DB_DRIVER_CLASS
              value: "org.postgresql.Driver"
            - name: DB_SCHEME
              value: "postgresql"
            - name: DB_HOST
              value: "postgresql"
            - name: DB_PORT
              value: "5432"
            - name: OM_DATABASE
              value: "openmetadata_db"
            - name: DB_USER
              value: "postgres"
            - name: DB_USER_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgresql-secret
                  key: password
            - name: AIRFLOW_HOST
              value: "http://airflow:8080"
            - name: SERVER_HOST_API_URL
              value: "http://openmetadata:8585/api"
            - name: ELASTICSEARCH_HOST
              value: "elasticsearch"
            - name: ELASTICSEARCH_PORT
              value: "9200"
            - name: ELASTICSEARCH_SCHEME
              value: "http"
            - name: SEARCH_TYPE
              value: "elasticsearch"
          resources:
            requests:
              cpu: "500m"
              memory: "1Gi"
            limits:
              cpu: "1000m"
              memory: "2Gi"
          livenessProbe:
            httpGet:
              path: /api/v1/system/version
              port: 8585
            initialDelaySeconds: 60
            periodSeconds: 15
            failureThreshold: 5
          readinessProbe:
            httpGet:
              path: /api/v1/system/version
              port: 8585
            initialDelaySeconds: 30
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: openmetadata
  namespace: ai-agent-platform
spec:
  selector:
    app: openmetadata
  ports:
    - name: http
      port: 8585
      targetPort: 8585
