apiVersion: v1
kind: Service
metadata:
  name: ai-agent-orchestrator
  namespace: ai-agent-platform
spec:
  selector:
    app: ai-agent-orchestrator
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
