httpx==0.27.0
# kafka-python の ApiVersions ネゴシエーションが Kafka 4.x (KRaft) ブローカーと
# 非互換で NodeNotReadyError/NoBrokersAvailable になるため、実ブローカーへの
# 管理操作 (トピック作成・削除・一覧等)も通知トピックの購読も、Python クライアント
# は使わず、コンテナイメージに同梱した kafka-topics.sh / kafka-console-consumer.sh
# (CLI) を subprocess 経由で呼び出す
# (tools/kafka/admin_tools.py, agent/orchestrator/notifications.py 参照)。
# Skupper経由の外部リスナー(9094)が不安定な場合のフォールバックとして、各サイト
# クラスタのブローカーPodへ直接 pods/exec するために使う(kubernetes exec
# サブリソースは WebSocket/SPDYベースのプロトコルで、httpxの単純なREST呼び出し
# では実装できないため、公式クライアントを使う)。
kubernetes==30.1.0
langchain-core==0.2.40
langchain-openai==0.1.25
openmetadata-ingestion==1.3.0
pydantic==1.10.17
structlog==24.1.0
# OpenTelemetry (Observable by Default 原則)
opentelemetry-api==1.25.0
opentelemetry-sdk==1.25.0
opentelemetry-exporter-otlp-proto-grpc==1.25.0
opentelemetry-instrumentation-httpx==0.46b0
# Prometheus metrics
prometheus-client==0.20.0
