server {
    listen 8080;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # 実行時に書き換える config.js だけは書き込み可能な /tmp から配信する
    location = /config.js {
        alias /tmp/runtime-config/config.js;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
